# Requires Python >= 3.10
# Note: For GPU acceleration, ensure Nvidia drivers are installed on the host system.

matplotlib==3.10.6
numpy==2.3.5
torch==2.10.0
torchvision==0.25.0
transformers==5.3.0
huggingface-hub==1.6.0
Pillow>=10.0.0